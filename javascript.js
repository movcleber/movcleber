const altura = 5;
const comprimento = 7;
const base = 2;
const forma = 'triangulo';
const forma2 = 'quadrado';


const area = altura * comprimento; 
const area2 = (altura * comprimento) / base;

if (forma === 'triangulo') {
    console.log(area);
} else  {
    console.log(area2);
}
